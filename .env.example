# 复制为 .env 并填入真实值（本地运行用；部署到 Render 时改为在控制台设置同名环境变量）
# 访问口令：好友打开网站时输入的「普通口令」，只能使用转换功能。
# 留空则首次启动自动生成并写入 ACCESS_CODE.txt
FLC_ACCESS_CODE=

# 管理员密钥（可选，强烈建议设置）：持有者可点亮「初始化授权」按钮并完成飞书 OAuth。
# 与 FLC_ACCESS_CODE 不同 —— 好友只拿普通口令，看不到/点不了初始化按钮；
# 你拿这个密钥，既能使用工具、也能唤起初始化授权。
# 不设置则默认等于 FLC_ACCESS_CODE（即同一口令既可用又可管理，兼容旧部署）。
FLC_ADMIN_CODE=

# 飞书自建应用凭证（在 open.feishu.cn 开发者后台「凭证与基础信息」获取）
FEISHU_APP_ID=cli_xxxxxxxxxxxxxxxx
FEISHU_APP_SECRET=xxxxxxxxxxxxxxxxxxxxxxxx

# 本服务的公网地址（用于拼 OAuth 回调）。本地测试填 http://localhost:8787
PUBLIC_BASE=http://localhost:8787

# 监听端口（默认 8787）
PORT=8787
